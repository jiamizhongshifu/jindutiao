# GaiYa v1.6 打包说明

## ⚠️ 当前问题

`dist\GaiYa-v1.6.exe` 文件被锁定,无法删除或覆盖。

## 🔧 解决方法

### 方法1: 重启电脑后打包 (推荐)

1. 保存所有工作
2. 重启电脑
3. 重启后立即打包:
   ```cmd
   cd c:\Users\Sats\Downloads\jindutiao
   python -m PyInstaller Gaiya.spec --noconfirm
   ```

### 方法2: 使用任务管理器结束进程

1. 按 `Ctrl+Shift+Esc` 打开任务管理器
2. 找到并结束以下进程:
   - **GaiYa-v1.6.exe**
   - 所有 **python.exe**
3. 尝试手动删除:
   ```
   c:\Users\Sats\Downloads\jindutiao\dist\GaiYa-v1.6.exe
   ```
4. 如果删除成功,运行打包:
   ```cmd
   cd c:\Users\Sats\Downloads\jindutiao
   python -m PyInstaller Gaiya.spec --noconfirm
   ```

### 方法3: 生成不同文件名的exe

1. 临时修改版本号:
   ```cmd
   cd c:\Users\Sats\Downloads\jindutiao
   notepad version.py
   ```

2. 在version.py中找到:
   ```python
   VERSION_PATCH = 8
   ```

   改为:
   ```python
   VERSION_PATCH = 99
   ```

3. 保存文件并打包:
   ```cmd
   python -m PyInstaller Gaiya.spec --noconfirm
   ```

4. 这将生成 `dist\GaiYa-v1.6.99.exe`

5. 打包完成后,恢复version.py:
   ```cmd
   git checkout version.py
   ```

6. 将新exe重命名:
   ```cmd
   cd dist
   ren GaiYa-v1.6.99.exe GaiYa-v1.6-new.exe
   ```

### 方法4: 使用Unlocker工具

1. 下载Unlocker: https://www.iobit.com/en/iobit-unlocker.php
2. 右键点击 `dist\GaiYa-v1.6.exe`
3. 选择 "IObit Unlocker"
4. 点击 "Unlock" 解锁文件
5. 删除文件
6. 运行打包命令

## ✅ 打包成功后的验证

打包完成后,检查文件:

```cmd
dir dist\*.exe
```

应该看到:
- 文件名: `GaiYa-v1.6.exe` 或 `GaiYa-v1.6.99.exe`
- 文件大小: 约82-85MB
- 修改日期: 今天 (2025-12-03)

## 🧪 测试新exe

运行新打包的exe:

```cmd
cd dist
GaiYa-v1.6-new.exe
```

或

```cmd
cd dist
GaiYa-v1.6.99.exe
```

测试步骤:
1. 打开应用
2. 进入"个人中心" → "会员订阅"
3. 选择任意套餐
4. 点击"前往支付"
5. 完成¥0.1支付
6. **观察**: 3-9秒内应自动弹窗"支付成功"并更新会员状态

## 📝 已包含的修复

新打包的exe包含以下修复 (Commits: cb79d80, 559f2e6, ee8a221, dc87803):

1. ✅ **主动查询机制** - 不依赖Z-Pay回调
2. ✅ **导入路径修复** - 解决应用崩溃
3. ✅ **param字段返回** - payment-query API返回param
4. ✅ **简单分隔符** - 使用 `user_id|plan_type` 代替JSON

## 🆘 如果仍无法打包

请提供以下信息:

1. 任务管理器截图 (显示所有python和GaiYa进程)
2. 尝试删除exe时的错误信息
3. 运行打包命令后的完整输出

---

**创建时间**: 2025-12-03
**Git Commit**: dc87803 (已推送)
**Vercel部署**: 已完成
