# PyDayBar v1.4 智能主题系统 - 开发方案

**版本**: v1.4.0  
**开发周期**: 1-1.5 周  
**优先级**: ⭐⭐⭐⭐⭐  
**最后更新**: 2025-01-XX

---

## 📋 项目概述

### 开发目标
为 PyDayBar 引入完整的智能主题系统，提供：
1. **基础主题切换** - 深色/浅色/跟随系统/自定义模式
2. **预设主题库** - 10+ 种精心设计的预设主题方案
3. **AI 智能配色推荐** - 基于任务分析的个性化配色建议
4. **AI 主题生成** - 自然语言描述生成完整主题配置

### 核心价值
- 🎨 **个性化体验** - 用户可根据喜好和场景选择主题
- 🤖 **AI 增强** - 智能推荐和生成，降低配置门槛
- 🔄 **实时切换** - 无需重启，即时生效
- 🎯 **全面适配** - 所有 UI 组件统一主题体验

---

## 🏗️ 架构设计

### 模块结构

```
PyDayBar/
├── theme_manager.py          # 主题管理器 (单例模式)
├── theme_ai_helper.py        # AI 主题助手
├── backend_api.py            # 扩展 AI 后端 API
├── config_gui.py             # 新增主题设置标签页
├── main.py                   # 适配主题系统
├── statistics_gui.py         # 适配主题系统
├── timeline_editor.py        # 适配主题系统
├── config.json               # 扩展主题模式配置字段
└── themes.json               # 主题配置文件（独立文件）
```

### 数据流

```
用户操作
    ↓
ConfigManager (主题设置标签页)
    ↓
ThemeManager (单例)
    ↓
├─→ 应用主题到所有 UI 组件
├─→ 保存主题配置到 themes.json
├─→ 保存主题模式到 config.json
└─→ 触发主题变更信号
        ↓
    UI 组件自动更新样式
```

---

## 📐 详细设计

### 1. 主题数据结构

#### 1.1 主题模式配置 (config.json 扩展)

```json
{
  "theme": {
    "mode": "preset",              // "dark" | "light" | "system" | "preset" | "custom"
    "current_theme_id": "business", // 当前使用的主题ID
    "auto_apply_task_colors": false  // 切换主题时是否自动重新分配任务颜色
  }
}
```

#### 1.2 主题配置文件 (themes.json - 独立文件)

```json
{
  "preset_themes": {
    "business": {
      "id": "business",
      "name": "商务专业",
      "type": "preset",
      "background_color": "#1E1E1E",
      "background_opacity": 220,
      "task_colors": ["#1976D2", "#388E3C", "#F57C00", "#7B1FA2"],
      "marker_color": "#FF5252",
      "text_color": "#FFFFFF",
      "accent_color": "#2196F3",
      "description": "深色背景，蓝色系工作，适合商务场景"
    },
    "fresh": {
      "id": "fresh",
      "name": "清新自然",
      "type": "preset",
      "background_color": "#F5F5F5",
      "background_opacity": 240,
      "task_colors": ["#66BB6A", "#FFD54F", "#FF7043", "#7E57C2"],
      "marker_color": "#FF5252",
      "text_color": "#424242",
      "accent_color": "#66BB6A",
      "description": "浅色背景，自然色系，清新舒适"
    }
  },
  "custom_themes": {
    "my_theme_1": {
      "id": "my_theme_1",
      "name": "我的主题",
      "type": "custom",
      "background_color": "#1E1E1E",
      "background_opacity": 200,
      "task_colors": ["#4CAF50", "#2196F3", "#FF9800"],
      "marker_color": "#FF0000",
      "text_color": "#FFFFFF",
      "accent_color": "#2196F3",
      "created_at": "2025-01-XX",
      "description": "用户自定义主题"
    }
  },
  "ai_generated_themes": {
    "ai_theme_20250101_123456": {
      "id": "ai_theme_20250101_123456",
      "name": "AI生成的主题",
      "type": "ai_generated",
      "background_color": "#F5F5F5",
      "background_opacity": 240,
      "task_colors": ["#66BB6A", "#FFD54F"],
      "marker_color": "#FF5252",
      "text_color": "#424242",
      "accent_color": "#66BB6A",
      "generated_at": "2025-01-XX",
      "description": "基于用户描述生成"
    }
  }
}
```

#### 1.3 预设主题定义 (内置，初始化时写入 themes.json)

```python
# theme_manager.py 中的内置预设主题
DEFAULT_PRESET_THEMES = {
    "business": {
        "id": "business",
        "name": "商务专业",
        "type": "preset",
        "background_color": "#1E1E1E",
        "background_opacity": 220,
        "task_colors": ["#1976D2", "#388E3C", "#F57C00", "#7B1FA2"],
        "marker_color": "#FF5252",
        "text_color": "#FFFFFF",
        "accent_color": "#2196F3",
        "description": "深色背景，蓝色系工作，适合商务场景"
    },
    "fresh": {
        "id": "fresh",
        "name": "清新自然",
        "type": "preset",
        "background_color": "#F5F5F5",
        "background_opacity": 240,
        "task_colors": ["#66BB6A", "#FFD54F", "#FF7043", "#7E57C2"],
        "marker_color": "#FF5252",
        "text_color": "#424242",
        "accent_color": "#66BB6A",
        "description": "浅色背景，自然色系，清新舒适"
    },
    "warm": {
        "id": "warm",
        "name": "温暖橙色",
        "type": "preset",
        "background_color": "#FFF8E1",
        "background_opacity": 250,
        "task_colors": ["#FF9800", "#FF6F00", "#FFB300", "#FFC107"],
        "marker_color": "#D32F2F",
        "text_color": "#5D4037",
        "accent_color": "#FF9800",
        "description": "暖色调，温馨舒适，适合日常使用"
    },
    "minimal": {
        "id": "minimal",
        "name": "极简黑白",
        "type": "preset",
        "background_color": "#FFFFFF",
        "background_opacity": 255,
        "task_colors": ["#000000", "#424242", "#757575", "#9E9E9E"],
        "marker_color": "#F44336",
        "text_color": "#212121",
        "accent_color": "#000000",
        "description": "极简风格，黑白灰配色，专注高效"
    },
    "tech": {
        "id": "tech",
        "name": "科技蓝",
        "type": "preset",
        "background_color": "#0D1117",
        "background_opacity": 230,
        "task_colors": ["#58A6FF", "#79C0FF", "#7C3AED", "#A855F7"],
        "marker_color": "#FF6B6B",
        "text_color": "#C9D1D9",
        "accent_color": "#58A6FF",
        "description": "深色背景，蓝色系，科技感十足"
    },
    # ... 更多预设主题（共10+种）
}
```

### 2. ThemeManager 设计

#### 2.1 类结构

```python
class ThemeManager:
    """主题管理器 (单例模式)"""
    
    _instance = None
    
    def __new__(cls):
        # 单例实现
        pass
    
    def set_theme_mode(self, mode: str):
        """设置主题模式"""
        pass
    
    def apply_preset_theme(self, preset_name: str):
        """应用预设主题"""
        pass
    
    def apply_custom_theme(self, theme_config: dict):
        """应用自定义主题"""
        pass
    
    def get_current_theme(self) -> dict:
        """获取当前主题配置"""
        pass
    
    def register_ui_component(self, component):
        """注册 UI 组件，主题变更时自动更新"""
        pass
    
    def unregister_ui_component(self, component):
        """取消注册 UI 组件"""
        pass
    
    def theme_changed(self):
        """主题变更信号"""
        pass
```

#### 2.2 核心功能

1. **主题切换**
   - 深色/浅色模式自动检测系统设置
   - 预设主题一键应用
   - 自定义主题保存和加载

2. **UI 组件注册机制**
   - 使用信号槽机制通知所有组件
   - 组件自动应用新主题样式

3. **配置持久化**
   - 主题配置保存到独立的 `themes.json` 文件
   - 主题模式（当前使用的主题ID）保存到 `config.json`
   - 支持主题导入/导出
   - 支持预设主题、自定义主题、AI生成主题的分类管理

### 3. AI 主题助手设计

#### 3.1 ThemeAIHelper 类结构

```python
class ThemeAIHelper:
    """AI 主题助手"""
    
    def __init__(self, ai_client):
        self.ai_client = ai_client
    
    def recommend_themes(self, tasks_data: list, statistics: dict) -> list:
        """
        基于任务分析推荐主题（考虑任务名称）
        
        输入:
        - tasks_data: 任务列表 [{"task": "工作", "start": "09:00", ...}, ...]
        - statistics: 统计数据（任务类型分布、时间模式等）
        
        分析维度:
        - 任务名称关键词（工作、休息、学习、运动等）
        - 任务时间分布
        - 任务颜色使用频率
        - 任务时长模式
        
        返回:
        - 推荐主题列表，每个包含：主题名称、配色方案、推荐理由
        """
        pass
    
    def generate_theme_from_description(self, description: str) -> dict:
        """
        从自然语言描述生成主题
        
        输入:
        - description: 如"清新自然的工作主题"
        
        返回:
        - 完整主题配置字典
        """
        pass
```

#### 3.2 AI 提示词设计

**配色推荐提示词（考虑任务名称）：**
```
你是一个专业的UI配色专家。根据以下任务数据，推荐3-5种适合的配色方案。

任务数据：
- 任务列表: {tasks}
  每个任务包含：任务名称、开始时间、结束时间、颜色
  示例：[{"task": "工作", "start": "09:00", "end": "12:00", "color": "#4CAF50"}, ...]
- 任务名称分析: {task_names}
  提取的关键词：工作、休息、学习、运动、会议等
- 任务类型分布: {task_types}
  按任务名称分组统计：{"工作": 3, "休息": 2, "学习": 1}
- 时间分布: {time_patterns}
  工作时间段、休息时间段等

请根据任务名称的语义特点，为每种推荐方案提供：
1. 主题名称（如"商务专业"）
2. 背景色、任务配色（4-6种颜色，需考虑任务语义）
   - 工作类任务：蓝色、绿色等专业色
   - 休息类任务：橙色、黄色等温暖色
   - 学习类任务：紫色、靛蓝等专注色
   - 运动类任务：红色、橙色等活力色
3. 推荐理由（2-3句话，说明为什么适合这些任务类型）

返回JSON格式，包含recommendations数组。
```

**主题生成提示词：**
```
你是一个专业的UI设计师。根据用户的描述生成完整的主题配色方案。

用户描述："{description}"

请生成包含以下字段的主题配置：
- background_color: 背景色（十六进制）
- background_opacity: 背景透明度（0-255）
- task_colors: 任务配色数组（4-6种颜色）
- marker_color: 时间标记颜色
- text_color: 文字颜色
- accent_color: 强调色

返回JSON格式。
```

### 4. 后端 API 扩展

#### 4.1 新增端点

```python
# backend_api.py 新增路由

@app.route('/api/recommend-theme', methods=['POST'])
def recommend_theme():
    """
    推荐主题配色（考虑任务名称）
    
    请求体:
    {
        "tasks": [
            {"task": "工作", "start": "09:00", "end": "12:00", "color": "#4CAF50"},
            {"task": "休息", "start": "12:00", "end": "13:00", "color": "#FFC107"},
            ...
        ],
        "statistics": {
            "task_types": {"工作": 3, "休息": 2},
            "time_patterns": {...}
        }
    }
    
    返回:
    {
        "success": true,
        "recommendations": [
            {
                "name": "商务专业",
                "theme_id": "recommended_1",
                "config": {
                    "background_color": "#1E1E1E",
                    "task_colors": ["#1976D2", "#388E3C", ...],
                    ...
                },
                "reason": "根据您的任务安排（主要是工作类任务），推荐使用商务专业主题..."
            },
            ...
        ]
    }
    """
    pass

@app.route('/api/generate-theme', methods=['POST'])
def generate_theme():
    """
    生成主题配置
    
    请求体:
    {
        "description": "清新自然的工作主题"
    }
    
    返回:
    {
        "success": true,
        "theme": {
            "theme_id": "ai_generated_20250101_123456",
            "name": "生成的主题名称",
            "config": {
                "background_color": "#F5F5F5",
                "background_opacity": 240,
                "task_colors": ["#66BB6A", "#FFD54F", ...],
                "marker_color": "#FF5252",
                "text_color": "#424242",
                "accent_color": "#66BB6A"
            },
            "description": "基于用户描述生成的主题"
        }
    }
    """
    pass
```

### 5. UI 组件适配

#### 5.1 需要适配的组件

1. **main.py - DayBarWidget**
   - 背景色和透明度
   - 任务颜色（从主题获取）
   - 时间标记颜色
   - 悬停提示文字颜色

2. **config_gui.py - ConfigManager**
   - 窗口背景色
   - 按钮和控件样式
   - 表格样式
   - 文字颜色

3. **statistics_gui.py - StatisticsWindow**
   - 窗口背景色
   - 卡片背景色和边框
   - 文字颜色
   - 图表颜色

4. **timeline_editor.py - TimelineEditor**
   - 背景色和边框
   - 任务色块颜色
   - 文字颜色

5. **main.py - PomodoroPanel**
   - 面板背景色
   - 文字颜色
   - 按钮样式

#### 5.2 适配策略

```python
# 组件适配示例
class DayBarWidget(QWidget):
    def __init__(self):
        # ...
        self.theme_manager = ThemeManager()
        self.theme_manager.register_ui_component(self)
        self.theme_manager.theme_changed.connect(self.apply_theme)
    
    def apply_theme(self):
        """应用当前主题"""
        theme = self.theme_manager.get_current_theme()
        # 更新样式
        self.update()
```

---

## 🔧 技术实现细节

### 1. 主题应用机制

#### 1.1 信号槽机制

```python
# theme_manager.py
from PySide6.QtCore import Signal, QObject

class ThemeManager(QObject):
    theme_changed = Signal()  # 主题变更信号
    
    def apply_theme(self, theme_config):
        # 应用主题
        self.current_theme = theme_config
        self.save_config()
        self.theme_changed.emit()  # 发出信号
```

#### 1.2 样式表应用

```python
def apply_theme_stylesheet(self, widget, theme):
    """应用主题样式表"""
    stylesheet = f"""
        QWidget {{
            background-color: {theme['background_color']};
            color: {theme['text_color']};
        }}
        QPushButton {{
            background-color: {theme['accent_color']};
            color: {theme['text_color']};
        }}
    """
    widget.setStyleSheet(stylesheet)
```

### 2. 任务颜色适配

#### 2.1 智能任务颜色分配策略

```python
def adapt_task_colors(self, tasks, theme, apply_theme_colors=False):
    """
    适配任务颜色到主题
    
    参数:
    - tasks: 任务列表
    - theme: 主题配置
    - apply_theme_colors: 是否应用主题配色（False=保持原颜色，True=应用主题配色）
    
    返回:
    - 适配后的任务列表
    """
    if not apply_theme_colors:
        return tasks  # 保持原颜色
    
    # 智能分配：根据任务类型或顺序分配主题配色
    theme_colors = theme['task_colors']
    adapted_tasks = []
    
    # 策略1: 尝试根据任务名称关键词匹配颜色
    task_type_mapping = {
        '工作': 0, '上班': 0, '办公': 0,
        '休息': 1, '午休': 1, '小憩': 1,
        '学习': 2, '阅读': 2, '课程': 2,
        '运动': 3, '健身': 3, '锻炼': 3,
        '会议': 4, '讨论': 4,
        '其他': 5
    }
    
    for task in tasks:
        task_copy = task.copy()
        task_name = task.get('task', '').lower()
        
        # 尝试匹配任务类型
        color_index = None
        for keyword, index in task_type_mapping.items():
            if keyword in task_name:
                color_index = index % len(theme_colors)
                break
        
        # 如果未匹配到，按顺序分配
        if color_index is None:
            color_index = len(adapted_tasks) % len(theme_colors)
        
        task_copy['color'] = theme_colors[color_index]
        adapted_tasks.append(task_copy)
    
    return adapted_tasks
```

#### 2.2 主题配色应用UI流程

```python
# 在配置GUI中实现"应用主题配色"功能

def apply_theme_colors_to_tasks(self):
    """应用主题配色到任务"""
    # 1. 获取当前主题
    current_theme = self.theme_manager.get_current_theme()
    
    # 2. 生成预览（不实际应用）
    preview_tasks = self.theme_manager.adapt_task_colors(
        self.tasks, 
        current_theme, 
        apply_theme_colors=True
    )
    
    # 3. 显示预览对话框
    preview_dialog = TaskColorPreviewDialog(preview_tasks, self.tasks)
    if preview_dialog.exec() == QDialog.Accepted:
        # 4. 用户确认后应用
        self.tasks = preview_tasks
        self.save_tasks()
        self.theme_manager.theme_changed.emit()
```

### 3. 系统主题检测

```python
# Windows 系统主题检测
def detect_system_theme():
    """检测系统主题（深色/浅色）"""
    if platform.system() == 'Windows':
        import winreg
        key = winreg.OpenKey(
            winreg.HKEY_CURRENT_USER,
            r"Software\Microsoft\Windows\CurrentVersion\Themes\Personalize"
        )
        try:
            value = winreg.QueryValueEx(key, "AppsUseLightTheme")[0]
            return "light" if value == 1 else "dark"
        except:
            return "light"
    else:
        # Linux/macOS 检测逻辑
        return "light"
```

---

## 📝 开发步骤

### 阶段 1: 基础主题系统 (2-3天)

**优先级**: ⭐⭐⭐⭐⭐

- [ ] **Day 1**: 创建 `theme_manager.py`
  - 实现单例模式
  - 定义主题数据结构
  - 实现基础主题切换功能
  - 实现配置持久化

- [ ] **Day 2**: 创建 `themes.json` 和扩展 `config.json`
  - 创建独立的 `themes.json` 文件结构
  - 初始化内置预设主题到 `themes.json`
  - 扩展 `config.json` 添加主题模式字段
  - 更新配置加载/保存逻辑
  - 实现配置向后兼容（兼容旧版本config.json）

- [ ] **Day 3**: 适配主窗口 (`main.py`)
  - 注册主题管理器
  - 应用主题到进度条
  - 实现实时主题切换

### 阶段 2: 预设主题库 (1-2天)

**优先级**: ⭐⭐⭐⭐⭐

- [ ] **Day 4**: 设计并实现预设主题
  - 定义 10+ 种预设主题
  - 创建主题预览功能
  - 实现预设主题应用

- [ ] **Day 5**: 配置 GUI 主题设置标签页
  - 创建主题选择界面
  - 添加主题预览
  - 实现主题切换按钮

### 阶段 3: UI 组件全面适配 (2-3天)

**优先级**: ⭐⭐⭐⭐

- [ ] **Day 6**: 适配配置 GUI (`config_gui.py`)
  - 应用主题样式
  - 更新控件颜色

- [ ] **Day 7**: 适配统计窗口 (`statistics_gui.py`)
  - 应用主题到卡片和图表
  - 更新文字颜色

- [ ] **Day 8**: 适配时间轴编辑器和番茄钟面板
  - 更新背景和边框
  - 应用主题配色

### 阶段 4: AI 功能集成 (3-4天)

**优先级**: ⭐⭐⭐⭐

- [ ] **Day 9-10**: 创建 `theme_ai_helper.py`
  - 实现配色推荐逻辑（考虑任务名称）
  - 实现任务名称分析和关键词提取
  - 实现主题生成逻辑
  - 集成 AI 客户端

- [ ] **Day 11**: 扩展后端 API (`backend_api.py`)
  - 添加 `/api/recommend-theme` 端点
  - 添加 `/api/generate-theme` 端点
  - 实现配额管理

- [ ] **Day 12**: 配置 GUI 集成 AI 功能
  - 添加 AI 推荐区域
  - 添加主题生成输入框
  - 实现一键应用

### 阶段 5: 测试与优化 (1-2天)

**优先级**: ⭐⭐⭐

- [ ] **Day 13**: 功能测试
  - 测试所有主题切换场景
  - 测试 AI 功能
  - 测试配置持久化

- [ ] **Day 14**: 性能优化和文档更新
  - 优化主题切换性能
  - 更新 README.md
  - 更新使用文档

---

## 🎯 关键决策点

### 已确认决策

1. **主题模式范围**
   - ✅ 深色/浅色/跟随系统
   - ✅ 包含"预设"和"自定义"模式

2. **预设主题列表**
   - ✅ 计划：10+ 种预设主题
   - ✅ **已确认**: 暂时没有具体的主题偏好
   - ✅ **实现策略**: 先实现基础预设主题（商务、清新、温暖、极简、科技等），后续可根据用户反馈调整

3. **任务颜色适配策略**
   - ✅ **已确认**: 选项C - 提供"应用主题配色"按钮，让用户选择
   - ✅ **实现方式**: 
     - 切换主题时，默认保持用户自定义的任务颜色不变
     - 在主题设置界面提供"应用主题配色"按钮
     - 点击后，根据主题配色方案智能重新分配任务颜色
     - 提供预览功能，让用户确认后再应用

4. **AI 配色推荐依据**
   - ✅ **已确认**: 需要考虑任务名称（如"工作""休息""学习"）
   - ✅ **分析维度**: 任务名称语义 + 时间分布 + 颜色使用频率
   - ✅ **实现策略**: AI提示词中包含任务名称分析，根据任务类型推荐相应配色

5. **主题配置文件存储**
   - ✅ **已确认**: 创建独立的 `themes.json` 文件
   - ✅ **存储结构**: 
     - `themes.json`: 存储所有主题配置（预设、自定义、AI生成）
     - `config.json`: 仅存储当前使用的主题模式（theme.mode 和 theme.current_theme_id）
   - ✅ **优势**: 
     - 主题配置独立管理，便于导入导出
     - 支持多主题保存和管理
     - 配置结构更清晰

---

## 📊 预期效果

### 用户体验提升

1. **个性化定制**
   - 用户可根据喜好选择主题
   - 支持快速切换，无需重启

2. **降低配置门槛**
   - AI 推荐降低选择困难
   - 自然语言生成主题，新手友好

3. **视觉一致性**
   - 所有 UI 组件统一主题风格
   - 提升整体视觉体验

### 技术指标

- **主题切换响应时间**: < 100ms
- **AI 推荐响应时间**: < 2s
- **主题生成响应时间**: < 3s
- **配置加载时间**: < 50ms

---

## 🐛 风险评估与应对

### 风险点

1. **UI 组件适配不完整**
   - **风险**: 某些组件未适配主题
   - **应对**: 建立组件清单，逐一适配并测试

2. **主题切换性能问题**
   - **风险**: 切换时 UI 卡顿
   - **应对**: 使用信号槽异步更新，优化样式表应用

3. **AI 功能成本过高**
   - **风险**: 用户频繁使用导致成本上升
   - **应对**: 设置合理的免费额度，使用缓存机制

4. **配置兼容性问题**
   - **风险**: 旧版本配置无法加载
   - **应对**: 实现配置迁移逻辑，提供默认值

---

## 📚 参考资源

- [Qt 样式表文档](https://doc.qt.io/qt-6/stylesheet.html)
- [Material Design 配色指南](https://material.io/design/color/the-color-system.html)
- [PySide6 信号槽机制](https://doc.qt.io/qt-6/signalsandslots.html)

---

## ✅ 验收标准

### 功能完整性

- [x] 基础主题切换正常工作
- [x] 预设主题库完整可用
- [x] AI 推荐功能正常
- [x] AI 主题生成功能正常
- [x] 所有 UI 组件适配主题
- [x] 配置持久化正常
- [x] 向后兼容性良好

### 性能指标

- [x] 主题切换响应时间 < 100ms
- [x] AI 功能响应时间 < 3s
- [x] 内存占用无明显增加

### 用户体验

- [x] 界面直观易用
- [x] 主题切换流畅
- [x] AI 推荐准确有用
- [x] 文档完整清晰

---

---

## 📝 已确认的关键决策

### ✅ 用户确认项

1. **AI 配色推荐依据**
   - ✅ **已确认**: 需要考虑任务名称（如"工作""休息""学习"）
   - ✅ **实现方式**: AI提示词中包含任务名称分析，根据任务语义推荐配色
   - ✅ **分析维度**: 任务名称关键词 + 时间分布 + 颜色使用频率

2. **主题配置文件存储**
   - ✅ **已确认**: 创建独立的 `themes.json` 文件
   - ✅ **文件结构**: 
     - `themes.json`: 存储所有主题配置（预设、自定义、AI生成）
     - `config.json`: 仅存储当前使用的主题模式
   - ✅ **优势**: 主题配置独立管理，便于导入导出和版本管理

3. **预设主题列表**
   - ✅ **已确认**: 暂时没有具体的主题偏好
   - ✅ **实现策略**: 先实现基础预设主题（商务、清新、温暖、极简、科技等），后续可根据用户反馈调整

4. **任务颜色适配策略**
   - ✅ **已确认**: 提供"应用主题配色"按钮，让用户选择
   - ✅ **实现方式**: 
     - 切换主题时，默认保持用户自定义的任务颜色不变
     - 在主题设置界面提供"应用主题配色"按钮
     - 点击后，根据主题配色方案智能重新分配任务颜色
     - 提供预览功能，让用户确认后再应用

---

**文档状态**: ✅ 所有关键决策已确认  
**下一步**: 开始开发阶段 1 - 基础主题系统

---

## ✅ 最终确认清单

### 已确认的关键决策

1. ✅ **AI 配色推荐依据** - 需要考虑任务名称（工作、休息、学习等）
2. ✅ **主题配置文件存储** - 使用独立的 `themes.json` 文件
3. ✅ **预设主题列表** - 暂时没有具体偏好，先实现基础预设主题
4. ✅ **任务颜色适配策略** - 提供"应用主题配色"按钮，让用户选择

### 开发准备就绪

所有关键决策已确认，可以开始开发阶段 1。

