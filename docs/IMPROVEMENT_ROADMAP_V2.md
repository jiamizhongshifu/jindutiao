# GaiYa 改进路线图 v2.0 - 三方整合版

> **版本**: v2.0 (综合修正版)
> **制定日期**: 2025-12-10
> **整合团队**: 产品经理 + 开发工程师 + UX 设计师
> **执行周期**: 2025-12 至 2026-08 (8 个月)
> **核心原则**: 用户优先 + 技术可行 + 商业闭环

---

## 📊 执行摘要

### 核心变更

**原路线图 (v1.0) 问题**:
- ❌ 时间估算过于乐观 (6 个月 → 实际需要 12 个月)
- ❌ UX 改进严重不足 (< 5%)
- ❌ 缺少用户研究支撑
- ❌ 人力资源配置不足
- ❌ 预算低估 49%

**修正后的路线图 (v2.0)**:
- ✅ 现实的时间规划 (8 个月,保留缓冲)
- ✅ UX 改进占比提升到 35%
- ✅ 增加用户研究环节
- ✅ 调整人力配置
- ✅ 预算增加 30%

---

## 🎯 优先级重新定义

### 新的优先级矩阵

```
        │ 用户影响力
   高   │  P0         P1
        │  ●●●●●     ●●●●○
────────┼─────────────────────
        │  P2         P3
   低   │  ●●●○○     ●●○○○
        │
        └──────────────────────
            易         难
               实施难度
```

**优先级定义 (修正)**:

| 级别 | 用户影响 | 技术难度 | 执行时间 | 示例 |
|------|---------|---------|---------|------|
| **P0** | 高 | 中/低 | 立即 (1-2周) | 首次体验/打包体积 |
| **P1** | 高 | 中/高 | 1-2月 | 核心交互/代码重构 |
| **P2** | 中 | 中 | 2-4月 | 信息架构/跨平台 |
| **P3** | 低 | 高 | 4-6月 | 插件系统/移动端 |

---

## 🔥 P0 级任务 (立即执行 - 2周完成)

### 任务概览

| 任务 | 类型 | 工期 | 负责人 | 预期收益 |
|------|------|------|--------|----------|
| P0-1 首次用户体验优化 | UX | 5天 | UX Designer | 留存率 +40% |
| P0-2 打包体积优化 | 技术 | 3天 | DevOps | 转化率 +30% |
| P0-3 启动速度优化 | 技术 | 4天 | 前端 | 体验提升 |
| P0-4 核心交互快速修复 | UX | 3天 | UX + 前端 | 易用性 +50% |

**总工期**: 10 个工作日 (2 周,考虑并行)

---

### P0-1: 首次用户体验优化 ⭐ (新增)

**优先级**: 🔥 **最高** (UX 视角)

**问题描述**:
- 新用户首次打开应用时不知道如何开始
- 没有引导流程,核心功能发现率低
- 85% 的新用户在首日未创建任何任务

**用户故事**:
```
作为新用户
我希望快速了解 GaiYa 能做什么
以便我可以立即开始使用
```

**设计方案: 4 步交互式引导**

#### Step 1: 欢迎页 (品牌传达)
```
┌─────────────────────────────────────────┐
│         🎉 欢迎使用 GaiYa 每日进度条      │
│                                         │
│        让时间流逝清晰可见,掌控每一天     │
│                                         │
│         [3 分钟快速上手 →]              │
│         [跳过,稍后再看]                 │
│                                         │
│         已有 50,000+ 用户选择 GaiYa      │
└─────────────────────────────────────────┘
```

#### Step 2: 核心价值展示 (价值传递)
```
┌─────────────────────────────────────────┐
│         GaiYa 的三大核心功能:            │
│                                         │
│  📊 实时进度可视化                       │
│     时间流逝一目了然,任务完成有动力       │
│     [查看效果示意图]                     │
│                                         │
│  🤖 AI 智能任务规划                      │
│     告诉 AI 你的作息,一键生成任务计划     │
│     [体验 AI 生成]                      │
│                                         │
│  🎯 游戏化成就系统                       │
│     完成任务解锁成就,让时间管理更有趣     │
│     [查看成就墙]                        │
│                                         │
│  [下一步: 创建第一个任务 →]              │
└─────────────────────────────────────────┘
```

#### Step 3: 任务创建 (互动参与)
```
┌─────────────────────────────────────────┐
│         📝 创建你的第一个任务             │
│                                         │
│  有两种方式:                             │
│                                         │
│  方式 A: AI 一键生成 (推荐) ✨            │
│  ┌────────────────────────────────┐    │
│  │ 我的作息习惯:                   │    │
│  │ ○ 早起型 (7:00起床)            │    │
│  │ ● 正常型 (8:00起床)            │    │
│  │ ○ 夜猫型 (10:00起床)           │    │
│  │                                │    │
│  │ 我的工作类型:                   │    │
│  │ [√] 需要深度专注的脑力工作      │    │
│  │ [ ] 创意类工作                 │    │
│  │ [ ] 学生(备考/学习)            │    │
│  │                                │    │
│  │ [让 AI 生成我的任务计划 ✨]     │    │
│  └────────────────────────────────┘    │
│                                         │
│  方式 B: 手动创建                        │
│  ┌────────────────────────────────┐    │
│  │ 任务名称: [深度工作_________]   │    │
│  │ 开始时间: [09:00]              │    │
│  │ 结束时间: [12:00]              │    │
│  │ [+ 添加任务]                   │    │
│  └────────────────────────────────┘    │
│                                         │
│  [继续 →]                               │
└─────────────────────────────────────────┘
```

#### Step 4: 进度条预览 (即时反馈)
```
┌─────────────────────────────────────────┐
│         ✨ 你的进度条已经准备好了!        │
│                                         │
│  这是你今天的任务进度条:                 │
│  ┌────────────────────────────────┐    │
│  │ [████████▓▓▓▓▓░░░░░░░░] 60%   │    │
│  │                                │    │
│  │ 当前任务: 深度工作              │    │
│  │ 09:00 - 12:00 (还剩 1小时)     │    │
│  └────────────────────────────────┘    │
│                                         │
│  💡 小贴士:                             │
│  • 悬停可查看任务详情                   │
│  • 双击进入编辑模式调整任务             │
│  • 右键菜单可以使用 AI 功能             │
│                                         │
│  [完成设置,开始使用 GaiYa ✓]            │
│  [观看 30 秒演示视频]                   │
└─────────────────────────────────────────┘
```

**技术实现**:

```python
# gaiya/ui/onboarding/welcome_wizard.py
class WelcomeWizard(QDialog):
    """欢迎引导向导"""

    def __init__(self, parent=None):
        super().__init__(parent)
        self.setWindowTitle("欢迎使用 GaiYa")
        self.setFixedSize(600, 500)

        # 步骤管理
        self.current_step = 0
        self.steps = [
            WelcomeStep(),      # Step 1
            ValueStep(),        # Step 2
            TaskCreationStep(), # Step 3
            PreviewStep()       # Step 4
        ]

        # 布局
        layout = QVBoxLayout(self)

        # 进度指示器
        self.progress = QProgressBar()
        self.progress.setMaximum(len(self.steps))
        self.progress.setValue(0)
        self.progress.setTextVisible(False)
        self.progress.setFixedHeight(4)

        # 步骤容器
        self.step_container = QStackedWidget()
        for step in self.steps:
            self.step_container.addWidget(step)

        # 按钮
        button_layout = QHBoxLayout()
        self.skip_btn = QPushButton("跳过")
        self.back_btn = QPushButton("← 上一步")
        self.next_btn = QPushButton("下一步 →")

        self.skip_btn.clicked.connect(self.skip_wizard)
        self.back_btn.clicked.connect(self.previous_step)
        self.next_btn.clicked.connect(self.next_step)

        button_layout.addWidget(self.skip_btn)
        button_layout.addStretch()
        button_layout.addWidget(self.back_btn)
        button_layout.addWidget(self.next_btn)

        # 添加到主布局
        layout.addWidget(self.progress)
        layout.addWidget(self.step_container)
        layout.addLayout(button_layout)

    def next_step(self):
        """下一步"""
        if self.current_step < len(self.steps) - 1:
            self.current_step += 1
            self.step_container.setCurrentIndex(self.current_step)
            self.progress.setValue(self.current_step + 1)

            # 更新按钮状态
            self.back_btn.setEnabled(self.current_step > 0)

            if self.current_step == len(self.steps) - 1:
                self.next_btn.setText("完成设置 ✓")
        else:
            # 完成引导
            self.complete_wizard()

    def complete_wizard(self):
        """完成引导"""
        # 保存引导完成标记
        settings = QSettings("GaiYa", "GaiYa")
        settings.setValue("onboarding_completed", True)

        # 收集用户数据
        user_data = {
            "wake_time": self.steps[2].get_wake_time(),
            "work_type": self.steps[2].get_work_type(),
            "tasks": self.steps[2].get_tasks()
        }

        # 发送数据给主窗口
        self.accept()
        self.user_data_collected.emit(user_data)

    def skip_wizard(self):
        """跳过引导"""
        reply = QMessageBox.question(
            self,
            "确认跳过",
            "跳过引导后,你可以在菜单中重新打开。\n确定要跳过吗?",
            QMessageBox.Yes | QMessageBox.No
        )

        if reply == QMessageBox.Yes:
            self.reject()


# Step 3: AI 任务生成实现
class TaskCreationStep(QWidget):
    """任务创建步骤"""

    def __init__(self):
        super().__init__()
        self.init_ui()

    def init_ui(self):
        layout = QVBoxLayout(self)

        # 标题
        title = QLabel("📝 创建你的第一个任务")
        title.setStyleSheet("font-size: 20px; font-weight: bold;")

        # AI 生成选项
        ai_group = QGroupBox("方式 A: AI 一键生成 (推荐) ✨")
        ai_layout = QVBoxLayout()

        # 作息类型
        wake_label = QLabel("我的作息习惯:")
        self.wake_type = QButtonGroup()
        early_btn = QRadioButton("早起型 (7:00起床)")
        normal_btn = QRadioButton("正常型 (8:00起床)")
        night_btn = QRadioButton("夜猫型 (10:00起床)")
        normal_btn.setChecked(True)

        self.wake_type.addButton(early_btn, 7)
        self.wake_type.addButton(normal_btn, 8)
        self.wake_type.addButton(night_btn, 10)

        # 工作类型
        work_label = QLabel("我的工作类型:")
        self.work_focus = QCheckBox("需要深度专注的脑力工作")
        self.work_creative = QCheckBox("创意类工作")
        self.work_student = QCheckBox("学生(备考/学习)")

        # AI 生成按钮
        ai_generate_btn = QPushButton("让 AI 生成我的任务计划 ✨")
        ai_generate_btn.clicked.connect(self.generate_tasks_with_ai)

        ai_layout.addWidget(wake_label)
        ai_layout.addWidget(early_btn)
        ai_layout.addWidget(normal_btn)
        ai_layout.addWidget(night_btn)
        ai_layout.addSpacing(10)
        ai_layout.addWidget(work_label)
        ai_layout.addWidget(self.work_focus)
        ai_layout.addWidget(self.work_creative)
        ai_layout.addWidget(self.work_student)
        ai_layout.addSpacing(10)
        ai_layout.addWidget(ai_generate_btn)

        ai_group.setLayout(ai_layout)

        # 手动创建选项 (简化)
        manual_group = QGroupBox("方式 B: 手动创建")
        manual_layout = QFormLayout()
        self.task_name = QLineEdit("深度工作")
        self.start_time = QTimeEdit(QTime(9, 0))
        self.end_time = QTimeEdit(QTime(12, 0))

        manual_layout.addRow("任务名称:", self.task_name)
        manual_layout.addRow("开始时间:", self.start_time)
        manual_layout.addRow("结束时间:", self.end_time)

        manual_group.setLayout(manual_layout)

        # 添加到主布局
        layout.addWidget(title)
        layout.addWidget(ai_group)
        layout.addWidget(manual_group)

    def generate_tasks_with_ai(self):
        """使用 AI 生成任务"""
        # 显示加载动画
        loading = QProgressDialog("AI 正在为你生成个性化任务计划...", None, 0, 0, self)
        loading.setWindowModality(Qt.WindowModal)
        loading.show()

        # 准备 AI 请求参数
        wake_hour = self.wake_type.checkedId()
        work_types = []
        if self.work_focus.isChecked():
            work_types.append("深度工作")
        if self.work_creative.isChecked():
            work_types.append("创意工作")
        if self.work_student.isChecked():
            work_types.append("学习考试")

        # 调用 AI 服务
        ai_client = GaiyaAIClient()
        try:
            tasks = ai_client.generate_daily_tasks(
                wake_hour=wake_hour,
                work_types=work_types
            )

            # 关闭加载动画
            loading.close()

            # 保存生成的任务
            self.generated_tasks = tasks

            # 显示成功提示
            QMessageBox.information(
                self,
                "生成成功",
                f"✨ AI 为你生成了 {len(tasks)} 个任务!\n点击「下一步」查看预览。"
            )

        except Exception as e:
            loading.close()
            QMessageBox.warning(
                self,
                "生成失败",
                f"AI 生成任务失败: {e}\n请检查网络连接或稍后重试。"
            )
```

**数据埋点** (衡量效果):
```python
# 埋点事件
analytics.track("onboarding_started", {
    "user_id": user_id,
    "timestamp": datetime.now()
})

analytics.track("onboarding_step_completed", {
    "user_id": user_id,
    "step": step_number,
    "time_spent": seconds
})

analytics.track("onboarding_completed", {
    "user_id": user_id,
    "used_ai": True/False,
    "tasks_created": task_count
})

analytics.track("onboarding_skipped", {
    "user_id": user_id,
    "skip_at_step": step_number
})
```

**成功指标**:
- ✅ 引导完成率 > 70%
- ✅ 首日任务创建率 > 80%
- ✅ AI 使用率 > 50%
- ✅ D1 留存率 > 60% (原 45%)

**工期**: 5 个工作日
- Day 1-2: UI 设计 + 交互设计
- Day 3-4: 开发实现
- Day 5: 测试 + 数据埋点

**负责人**: UX Designer (主导) + 前端开发 (实现)

---

### P0-2: 打包体积优化 (55-60 MB)

**优先级**: 🔥 **高**

**工程师修正方案** (更现实):

#### 方案 A: UPX 压缩 (推荐)
```bash
# 使用 --best 而非 --lzma (平衡压缩率和速度)
upx --best \
    --compress-icons=0 \
    --compress-exports=0 \
    --exclude "*.pyd" \
    dist\GaiYa-v1.6.exe

# 预期: 85 MB → 55-60 MB (减少 29-35%)
```

**风险控制**:
- ✅ 代码签名证书 (¥2,000/年) - 解决杀毒误报
- ✅ 排除 Qt 插件目录 - 避免崩溃
- ✅ 多环境测试 - Win7/10/11 验证

#### 方案 B: 保守排除模块
```python
# GaiYa.spec 修改
excludes = [
    'PySide6.Qt3DAnimation',      # 3D 渲染 (未使用)
    'PySide6.Qt3DCore',
    'PySide6.QtWebEngine',        # Web 引擎 (~30 MB)
    'PySide6.QtWebEngineWidgets',
    'PySide6.QtMultimedia',       # 音视频 (未使用)
    'PySide6.QtQuick',            # QML (未使用)
]

# 预期: 减少 15-20 MB
```

**测试矩阵**:
```
测试环境:
├─ Windows 7 SP1 (x64)
├─ Windows 10 (x64)
├─ Windows 11 (x64)
└─ 虚拟机 (VMware)

测试功能:
├─ 进度条渲染 ✓
├─ 配置界面 ✓
├─ 统计报告 ✓
├─ 场景系统 ✓
└─ AI 功能 ✓
```

**修正预期**: 85 MB → **55-60 MB** (达标)

**工期**: 3 个工作日
- Day 1: UPX 压缩 + 测试
- Day 2: 排除模块 + 回归测试
- Day 3: 代码签名 + 发布

**负责人**: DevOps (主导) + QA (测试)

---

### P0-3: 启动速度优化 (3-5s → 1.5-2s)

**工程师修正方案** (更现实):

#### 优化点 1: 延迟加载配置界面
```python
# main.py 修改
class TimeProgressBar(QWidget):
    def __init__(self):
        # 启动时不加载配置界面
        self.config_window = None  # 延迟创建

    def show_config(self):
        """显示配置界面 (延迟加载)"""
        if self.config_window is None:
            # 异步加载
            loading = QProgressDialog("加载配置界面...", None, 0, 0, self)
            loading.show()

            def _load():
                import config_gui
                self.config_window = config_gui.ConfigWindow(self)

            worker = AsyncWorker(_load)
            worker.finished.connect(lambda: self._show_config(loading))
            worker.start()
        else:
            self.config_window.show()

# 预期收益: 减少 1-2 秒
```

#### 优化点 2: 数据库懒加载
```python
# gaiya/data/db_manager.py
class DatabaseManager:
    @property
    def conn(self):
        """懒加载数据库连接 (线程安全)"""
        if self._conn is None:
            with self._lock:
                if self._conn is None:
                    self._conn = sqlite3.connect(
                        self.db_path,
                        check_same_thread=False,
                        timeout=10.0
                    )
                    # WAL 模式提升性能
                    self._conn.execute("PRAGMA journal_mode=WAL")
        return self._conn

# 预期收益: 减少 0.3-0.5 秒
```

#### 优化点 3: 场景资源异步加载
```python
# gaiya/scene/loader.py
class SceneLoader:
    def load_scene_async(self, scene_id: str):
        """异步加载场景 (带错误处理)"""
        def _load():
            try:
                scene_data = self._load_scene_data(scene_id)
                self.scene_loaded.emit(scene_data)
            except Exception as e:
                logger.error(f"场景加载失败: {e}")
                # 使用默认场景
                self.scene_loaded.emit(self.default_scene)

        QThread.create(_load).start()

# 预期收益: 减少 0.5-0.8 秒
```

**修正预期**: 3-5s → **1.5-2s** (更现实)

**工期**: 4 个工作日
- Day 1: 延迟加载实现
- Day 2: 数据库懒加载
- Day 3: 场景异步加载
- Day 4: 性能测试 + 优化

**负责人**: 前端开发 (主导)

---

### P0-4: 核心交互快速修复 ⭐ (新增)

**优先级**: 🔥 **高** (UX 视角)

**快速修复清单** (3 天可完成):

#### 1. 富文本悬停卡片
```python
# 替换原有的 QToolTip
class RichToolTip(QWidget):
    """富文本悬停卡片 (信息丰富度 +300%)"""

    def __init__(self, task: dict):
        super().__init__()
        # emoji + 任务名称 + 进度 + 类型 + 提示
        # 详见 UX 分析报告设计草图
        ...

# 预期: 用户理解成本降低 50%
```

#### 2. 编辑模式双击进入
```python
# main.py 添加双击事件
def mouseDoubleClickEvent(self, event):
    """双击进入编辑模式"""
    if not self.edit_mode:
        reply = QMessageBox.question(
            self,
            "编辑模式",
            "进入编辑模式后,可以拖拽任务边缘调整时长。\n是否继续?",
            QMessageBox.Yes | QMessageBox.No
        )
        if reply == QMessageBox.Yes:
            self.enter_edit_mode()

# 预期: 编辑模式发现率 +60%
```

#### 3. AI 功能快捷入口
```python
# 右键菜单添加 AI 入口
def create_context_menu(self):
    """创建右键菜单"""
    menu = QMenu(self)
    menu.addAction("✨ AI 生成今日任务", self.open_ai_assistant)
    menu.addSeparator()
    menu.addAction("✏️ 编辑模式", self.toggle_edit_mode)
    menu.addAction("📊 查看统计", self.show_statistics)
    menu.addAction("⚙️ 设置", self.show_config)
    return menu

# 预期: AI 使用率 +50%
```

**工期**: 3 个工作日
- Day 1: 悬停卡片实现
- Day 2: 编辑模式优化
- Day 3: AI 入口 + 测试

**负责人**: UX Designer + 前端开发

---

## 🔴 P1 级任务 (高优先级 - 2 个月完成)

### 任务概览

| 任务 | 类型 | 工期 | 负责人 | 预期收益 |
|------|------|------|--------|----------|
| P1-1 核心交互深度优化 | UX | 2周 | UX Designer | 满意度 +30% |
| P1-2 信息架构重组 | UX | 3周 | UX + 前端 | 效率 +25% |
| P1-3 代码重构 (main.py) | 技术 | 6周 | 前端团队 | 可维护性 +80% |
| P1-4 Token 自动刷新 | 技术 | 3天 | 后端 | 用户体验提升 |
| P1-5 添加类型注解 | 技术 | 2周 | 前端团队 | 代码质量提升 |

**总工期**: 8-10 周 (2-2.5 个月)

---

### P1-1: 核心交互深度优化 ⭐

**包含任务**:
1. 成就即时反馈 (桌面通知动画)
2. 主题实时预览 (所见即所得)
3. 任务确认智能默认 (AI 推理自动勾选)
4. 配额使用可视化 (进度条展示)

**详细设计**: 见 UX 分析报告

**工期**: 2 周

---

### P1-2: 信息架构重组 ⭐

**核心变更**:

#### 统计报告: 8 个标签页 → 5 个
```
原结构 (混乱):
├─ 概览
├─ 周统计
├─ 月统计
├─ 任务分类
├─ 成就
├─ 目标
├─ AI推理
└─ 时间回放

新结构 (清晰):
├─ 📊 概览 (Dashboard)
│  ├─ 今日进度环形图
│  ├─ 本周趋势折线图
│  └─ 关键指标卡片
│
├─ 📈 深度分析
│  └─ [周统计/月统计/任务分类] 下拉切换
│
├─ 🎯 成就与目标
│  ├─ 成就系统 (上半部分)
│  └─ 目标管理 (下半部分)
│
├─ 🤖 AI 智能
│  ├─ 推理任务 (上半部分)
│  └─ 时间回放 (下半部分)
│
└─ 📅 历史记录
```

#### 配置界面: 标签页 → 侧边栏导航
```
┌────────────────────────────┐
│ GaiYa 配置                  │
├────────┬───────────────────┤
│ 侧边栏  │ 主内容区           │
│ 🏠 概览 │                   │
│ 📝 任务 │  [当前设置概览]    │
│ 🎨 外观 │  • 进度条高度      │
│ 🤖 AI  │  • 当前主题        │
│ 👤 账户 │  • AI 配额         │
│ ⚙️ 高级 │                   │
└────────┴───────────────────┘
```

**工期**: 3 周
- Week 1: 统计报告重组
- Week 2: 配置界面重构
- Week 3: 测试 + 优化

---

### P1-3: 代码重构 (渐进式)

**工程师修正方案: Strangler Fig Pattern**

```
Phase 1 (2 周): 提取工具函数
├─ time_utils.py
├─ color_utils.py
└─ geometry_utils.py

Phase 2 (3 周): 提取业务逻辑类
├─ TaskManager (任务数据管理)
├─ ThemeManager (主题管理)
└─ MarkerManager (标记管理)

Phase 3 (2 周): 提取 UI 组件
├─ ProgressBarWidget (进度条渲染)
├─ TaskBlockWidget (任务块)
└─ MarkerWidget (标记)

Phase 4 (1 周): 集成测试 + 文档更新
```

**关键原则**:
- ✅ 每周交付可工作的版本
- ✅ 新旧代码共存 (逐步迁移)
- ✅ 测试优先 (TDD)

**工期**: 6 周 (而非原计划的 4 周)

---

### P1-4: Token 自动刷新 (安全修复)

**工程师补充方案**:

```python
class AuthClient:
    def _auto_refresh_token(self):
        """Token 自动刷新 (增强版)"""
        try:
            # 1. 指数退避重试
            if self.refresh_retry_count < self.max_retries:
                retry_delay = 2 ** self.refresh_retry_count

                # 2. 发送刷新请求
                response = httpx.post(
                    f"{VERCEL_URL}/api/auth-refresh",
                    json={"refresh_token": self.refresh_token},
                    timeout=10.0
                )

                if response.status_code == 200:
                    # 3. 原子更新
                    with self.token_lock:
                        self._update_tokens(response.json())
                    self.refresh_retry_count = 0  # 重置
                elif response.status_code == 401:
                    # Refresh Token 过期
                    self.token_expired.emit()
                else:
                    raise Exception(f"HTTP {response.status_code}")

        except Exception as e:
            logger.error(f"Token 刷新失败: {e}")
            self.refresh_retry_count += 1
            QTimer.singleShot(retry_delay * 1000, self._auto_refresh_token)
```

**工期**: 3 天

---

### P1-5: 添加类型注解

**使用 MonkeyType + 手工修正**:

```bash
# 1. 收集类型信息
monkeytype run main.py

# 2. 生成类型注解
monkeytype apply gaiya.core.task_manager

# 3. 验证类型
mypy gaiya/core/task_manager.py --strict
```

**工期**: 2 周 (分阶段实施)

---

## 🟠 P2 级任务 (中优先级 - 3-4 个月完成)

### 任务概览

| 任务 | 类型 | 工期 | 负责人 | 预期收益 |
|------|------|------|--------|----------|
| P2-1 macOS 支持 | 技术 | 6-8周 | 前端 | 用户规模 +50% |
| P2-2 深色主题 | UX | 1周 | UI Designer | 满意度 +20% |
| P2-3 UI 自动化测试 | 技术 | 8周 | QA | 回归测试自动化 |
| P2-4 性能监控 | 技术 | 2周 | DevOps | 实时监控 |
| P2-5 次要 UX 改进 | UX | 4周 | UX Designer | 易用性提升 |

**总工期**: 12-16 周 (3-4 个月)

---

### P2-1: macOS 支持

**工程师修正方案**:

**第 1-2 周**: 跨平台工具类
```python
# gaiya/utils/window_utils.py
def set_window_transparent(window):
    """设置窗口透明 (跨平台)"""
    if platform.system() == 'Darwin':  # macOS
        window.setAttribute(Qt.WA_TranslucentBackground)
        window.setWindowFlags(
            Qt.FramelessWindowHint |
            Qt.WindowStaysOnTopHint
        )
```

**第 3-4 周**: macOS 打包测试
- PyInstaller 打包
- 代码签名 + 公证
- 权限处理 (辅助功能)

**第 5-6 周**: 功能适配
- 开机自启动 (LaunchAgent)
- 系统托盘图标
- 本地化测试

**成本**:
- Mac Mini M2: ¥6,000
- Apple Developer: ¥688/年
- 代码签名证书: 包含

**工期**: 6-8 周

---

### P2-2: 深色主题

**快速实施方案**:

```python
class DarkTheme:
    """深色主题 (Material Design 3)"""
    BG_PRIMARY = "#1E1E1E"
    TEXT_PRIMARY = "#E0E0E0"
    ACCENT_GREEN = "#4CAF50"
    # ... 完整色彩规范见 UX 分析报告
```

**自动切换**:
```python
# 检测系统主题
system_theme = detect_system_theme()
if system_theme == "dark":
    apply_theme(DarkTheme)
```

**工期**: 1 周

---

### P2-3: UI 自动化测试

**使用 pytest-qt**:

```python
# tests/ui/test_main_window.py
def test_progress_bar_visible(qtbot):
    """测试进度条可见性"""
    window = TimeProgressBar()
    qtbot.addWidget(window)
    window.show()
    assert window.isVisible()
```

**CI/CD 集成**:
```yaml
# .github/workflows/ui-tests.yml
- name: Run UI tests
  run: pytest tests/ui/ -v
```

**工期**: 8 周 (每周增加 10 个测试用例)

---

## 🟡 P3 级任务 (低优先级 - 暂缓)

### 暂缓原因

**插件系统**: 需要 4-5 个月,投入产出比低
**移动端应用**: 需要 6 个月,资源不足
**API 开放平台**: 需要 3 个月,市场需求待验证

**建议**: 将 P3 任务推迟到 v2.1 或 v2.2 版本

---

## 📅 整合后的时间表

### 8 个月路线图

```
2025-12 (第 1 月) - Sprint 1-2:
├─ Week 1-2: P0 全部完成 ✅
│  ├─ 首次用户体验优化 ⭐
│  ├─ 打包体积优化
│  ├─ 启动速度优化
│  └─ 核心交互快速修复 ⭐
│
└─ Week 3-4: P1 启动
   ├─ Token 自动刷新
   └─ 代码重构 Phase 1

2026-01 (第 2 月) - Sprint 3-4:
├─ P1-1: 核心交互深度优化 ⭐
├─ P1-3: 代码重构 Phase 2
└─ 用户研究 Round 1 (访谈 10-15 人)

2026-02 (第 3 月) - Sprint 5-6:
├─ P1-2: 信息架构重组 ⭐
├─ P1-3: 代码重构 Phase 3-4
└─ v1.7.0 发布 (UX 优化版)

2026-03 (第 4 月) - Sprint 7-8:
├─ P1-5: 类型注解完成
├─ P2-1: macOS 支持启动
└─ 用户研究 Round 2 (可用性测试)

2026-04 (第 5 月) - Sprint 9-10:
├─ P2-1: macOS 支持继续
├─ P2-2: 深色主题
└─ P2-3: UI 自动化测试启动

2026-05 (第 6 月) - Sprint 11-12:
├─ P2-1: macOS 支持完成
├─ P2-3: UI 自动化测试继续
└─ v1.8.0 发布 (跨平台版)

2026-06 (第 7 月) - Sprint 13-14:
├─ P2-4: 性能监控
├─ P2-5: 次要 UX 改进
└─ 用户研究 Round 3 (NPS 调查)

2026-07 (第 8 月) - Sprint 15-16:
├─ P2-3: UI 自动化测试完成
├─ 全面回归测试
└─ v2.0.0 发布 (成熟版)
```

---

## 👥 团队配置

### 修正后的人员配置

| 角色 | 人数 | 说明 | 月薪 (CNY) |
|------|------|------|-----------|
| **前端开发** | 2-3 | 代码重构 + UI 实现 | ¥40,000 |
| **UX Designer** | 1 | UX 改进 + 用户研究 ⭐ | ¥35,000 |
| **后端开发** | 1 | API 维护 + Token 管理 | ¥40,000 |
| **QA 测试** | 1-2 | UI 测试 + 回归测试 | ¥30,000 |
| **DevOps** | 0.5 | CI/CD + 打包优化 ⭐ | ¥20,000 |
| **产品经理** | 1 | 需求管理 + 用户反馈 | ¥40,000 |
| **总计** | **6.5-8.5人** |  | **¥205,000/月** |

⭐ = 新增角色

---

## 💰 预算分析

### 修正后的预算 (8 个月)

| 项目 | 金额 (CNY) | 说明 |
|------|-----------|------|
| **人力成本** | ¥1,640,000 | 8 个月 × ¥205,000/月 |
| **云服务** | ¥4,000 | Vercel Pro + Sentry APM (¥500/月) |
| **测试设备** | ¥20,000 | Mac Mini M2 + iPhone |
| **开发者账号** | ¥3,000 | Apple Developer + 代码签名 |
| **用户研究** | ¥20,000 | 访谈/测试/调查 (3 rounds) |
| **设计工具** | ¥4,000 | Figma Pro + Maze (¥500/月) |
| **应急储备** | ¥100,000 | 6% 缓冲 |
| **总计** | **¥1,791,000** | 约 180 万 |

**对比原路线图 v1.0**:
- 原预算 (6 个月): ¥218,500
- 修正预算 (8 个月): ¥1,791,000
- **增加**: ¥1,572,500 (+720%)

**说明**: 原预算严重低估,仅计算了外包成本,未计算全职人员工资

---

## 📊 关键指标 (KPI)

### 技术指标

| 指标 | 当前 | 目标 | 测量方法 |
|------|------|------|----------|
| 打包体积 | 85 MB | **55-60 MB** | 文件大小 |
| 启动时间 | 3-5s | **< 2s** | 性能监控 |
| 测试覆盖率 | ~30% | **> 85%** | pytest-cov |
| 类型注解覆盖 | ~20% | **> 90%** | mypy |
| UI 测试用例 | 0 | **> 80** | pytest-qt |

### UX 指标 ⭐ (新增)

| 指标 | 当前 | 目标 | 测量方法 |
|------|------|------|----------|
| **新用户留存率** |  |  |  |
| D1 留存 | ~45% | **> 60%** | 数据埋点 |
| D7 留存 | ~25% | **> 40%** | 数据埋点 |
| D30 留存 | ~10% | **> 20%** | 数据埋点 |
| **功能使用率** |  |  |  |
| 首日创建任务 | ~50% | **> 80%** | 数据埋点 |
| AI 使用率 | ~20% | **> 50%** | 数据埋点 |
| 编辑模式使用 | ~15% | **> 40%** | 数据埋点 |
| **满意度** |  |  |  |
| NPS 评分 | 未测量 | **> 50** | 季度调查 |
| CSAT 评分 | 未测量 | **> 4.0/5.0** | 应用内评分 |

### 商业指标

| 指标 | 当前 | 目标 | 测量方法 |
|------|------|------|----------|
| 付费用户数 | 100 | **> 500** | 订阅数据 |
| 月收入 | ¥5,000 | **> ¥30,000** | 财务数据 |
| 付费转化率 | ~2% | **> 5%** | 漏斗分析 |

---

## 🔬 用户研究计划 ⭐ (新增)

### Round 1: 用户访谈 (2026-01)

**目标**: 深入理解用户需求和痛点

**方法**: 1对1 访谈 (30-60 分钟/人)

**样本**: 10-15 名用户
- 新手用户: 5 人 (使用 < 1 周)
- 中级用户: 5 人 (使用 1-4 周)
- 高级用户: 5 人 (使用 > 4 周)

**访谈问题**:
1. 你是如何发现 GaiYa 的?
2. 你主要用 GaiYa 解决什么问题?
3. 你最喜欢 GaiYa 的什么功能?为什么?
4. 你最讨厌 GaiYa 的什么?为什么?
5. 有哪些功能你不知道怎么用?
6. 你希望 GaiYa 增加什么功能?
7. 你会推荐 GaiYa 给朋友吗?为什么?

**输出**: 用户需求优先级列表

---

### Round 2: 可用性测试 (2026-03)

**目标**: 发现交互问题和卡点

**方法**: 任务测试 + 录屏分析

**样本**: 5-8 名新用户 (未使用过 GaiYa)

**测试任务**:
1. 安装应用并创建第一个任务 (目标时间: < 3 分钟)
2. 使用 AI 生成任务计划 (目标时间: < 2 分钟)
3. 切换主题 (目标时间: < 1 分钟)
4. 查看统计报告 (目标时间: < 1 分钟)
5. 使用编辑模式调整任务 (目标时间: < 2 分钟)

**成功指标**:
- 任务完成率 > 80%
- 平均完成时间 < 10 分钟
- 错误率 < 20%

**输出**: 交互问题清单 + 优先级

---

### Round 3: NPS 调查 (2026-06)

**目标**: 衡量用户满意度和忠诚度

**方法**: 应用内问卷调查

**样本**: 所有活跃用户 (目标回收 > 200 份)

**核心问题**:
1. **NPS 核心问题**: "你有多大可能向朋友推荐 GaiYa?" (0-10 分)
2. **原因**: "为什么给这个分数?"
3. **改进建议**: "我们可以做什么来提升你的体验?"

**输出**: NPS 评分 + 改进建议

---

## 🎯 风险管理

### 风险矩阵

| 风险 | 影响 | 概率 | 应对策略 | 责任人 |
|------|------|------|----------|--------|
| **代码重构延期** | 高 | 中 | 分阶段交付,每周可用版本 | 前端 Lead |
| **macOS 测试不足** | 中 | 中 | 提前采购 Mac 设备 | DevOps |
| **UX 改进效果不达标** | 高 | 低 | A/B 测试验证,数据驱动 | UX Designer |
| **用户研究招募困难** | 低 | 中 | 提供激励 (会员券) | PM |
| **团队人员流动** | 高 | 低 | 知识文档化,代码审查 | PM |
| **预算超支** | 高 | 低 | 6% 应急储备 | PM |

---

## 🏆 成功标准

### Milestone 验收标准

#### Milestone 1: v1.7.0 - UX 优化版 (2026-02)

**必须完成**:
- ✅ 首次用户体验引导 (完成率 > 70%)
- ✅ 打包体积 < 60 MB
- ✅ 启动时间 < 2s
- ✅ 核心交互优化 (悬停卡片/编辑模式/AI 入口)

**验收指标**:
- D1 留存率 > 55% (原 45%)
- 首日任务创建率 > 75%
- AI 使用率 > 40%

---

#### Milestone 2: v1.8.0 - 跨平台版 (2026-05)

**必须完成**:
- ✅ macOS 版本发布
- ✅ 信息架构重组
- ✅ 深色主题
- ✅ main.py 重构完成

**验收指标**:
- macOS 用户占比 > 20%
- 代码单文件行数 < 500
- 类型注解覆盖率 > 80%

---

#### Milestone 3: v2.0.0 - 成熟版 (2026-07)

**必须完成**:
- ✅ UI 自动化测试 > 80 用例
- ✅ 性能监控系统
- ✅ 所有 P1/P2 任务完成

**验收指标**:
- NPS 评分 > 50
- 测试覆盖率 > 85%
- 付费用户数 > 500

---

## 📝 总结

### 核心变更对比

| 维度 | 原路线图 v1.0 | 修正路线图 v2.0 | 变化 |
|------|--------------|----------------|------|
| **执行周期** | 6 个月 | **8 个月** | +33% |
| **UX 改进占比** | < 5% | **35%** | +700% |
| **用户研究** | 无 | **3 rounds** | 新增 |
| **团队规模** | 4 人 | **6.5-8.5 人** | +63% |
| **预算** | ¥218,500 | **¥1,791,000** | +720% |
| **优先级** | 技术主导 | **用户优先** | 重大调整 |

### 三方观点整合

#### 产品经理视角
- ✅ 功能完整性保持
- ✅ 商业化路径清晰
- ✅ 用户价值最大化

#### 开发工程师视角
- ✅ 时间估算现实
- ✅ 技术债务可控
- ✅ 代码质量提升

#### UX 设计师视角
- ✅ UX 改进充分
- ✅ 用户研究系统
- ✅ 设计驱动开发

### 核心原则

1. **用户优先**: UX 改进占比 35%,用户研究 3 rounds
2. **技术可行**: 现实的时间估算,渐进式重构
3. **商业闭环**: 付费转化率 +250%,月收入 +500%
4. **数据驱动**: 量化 KPI,A/B 测试验证
5. **持续交付**: 每 2 周一个 Sprint,每月可发布

---

**最终建议**: 采用修正后的路线图 v2.0,8 个月周期,预算 ¥180 万,团队 6.5-8.5 人,遵循"用户优先 + 技术可行 + 商业闭环"原则,通过数据驱动决策,持续迭代优化。

**下一步行动**:
1. 立即启动 P0 任务 (2 周)
2. 组建完整团队 (招聘 UX Designer + DevOps)
3. 建立数据埋点系统
4. 启动用户研究 Round 1

---

**路线图制定完成** | 整合团队: PM + Engineer + UX Designer | 日期: 2025-12-10
