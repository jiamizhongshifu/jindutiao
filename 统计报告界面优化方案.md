# 统计报告界面风格优化方案

> **目标**: 使统计报告界面与配置界面风格保持一致,提升整体用户体验

## 📋 当前问题分析

根据用户截图,当前统计报告界面存在以下问题:

1. **背景色偏暗** (#2c2c2c暗灰色) - 与配置界面的明亮风格不符
2. **卡片缺少阴影** - 缺少现代化UI的立体感
3. **色彩对比度不足** - 文字在暗色背景上阅读体验欠佳
4. **按钮样式简单** - 缺少配置界面那种精致的按钮设计

## 🎨 目标风格参考

参照 `config_gui.py` 的设计风格:

- **背景色**: `#FFFFFF` (纯白) 或 `#F5F5F5` (浅灰)
- **卡片背景**: `#FFFFFF` (白色卡片)
- **卡片边框**: `1px solid #E0E0E0` + `border-radius: 8px`
- **主色调**: `#2196F3` (Material Blue)
- **成功色**: `#4CAF50` (Material Green)
- **警告色**: `#FF9800` (Material Orange)
- **危险色**: `#F44336` (Material Red)

## 🔧 具体优化项

### 1. 窗口背景

```python
# 当前
self.setStyleSheet(f"""
    QWidget {{
        background-color: {bg_color};  # 可能是暗色
        color: {text_color};
    }}
""")

# 优化为
self.setStyleSheet("""
    StatisticsWindow {
        background-color: #F5F5F5;
    }
    QWidget {
        color: #2C3E50;
    }
""")
```

### 2. 标签页样式

```python
# 优化标签页样式,与配置界面统一
self.tab_widget.setStyleSheet("""
    QTabWidget::pane {
        border: 1px solid #E0E0E0;
        background: #FFFFFF;
        border-radius: 4px;
    }
    QTabBar::tab {
        padding: 10px 20px;
        margin-right: 2px;
        background: #F5F5F5;
        color: #2C3E50;
        border: 1px solid #E0E0E0;
        border-bottom: none;
        border-top-left-radius: 4px;
        border-top-right-radius: 4px;
    }
    QTabBar::tab:hover {
        background: #EEEEEE;
    }
    QTabBar::tab:selected {
        background: #FFFFFF;
        color: #2196F3;
        border-bottom: 2px solid #2196F3;
    }
""")
```

### 3. 提示卡片优化

```python
# 当前的蓝色提示卡片
hint_label.setStyleSheet("""
    QLabel {
        background-color: #e3f2fd;
        border: 1px solid #90caf9;
        border-radius: 4px;
        padding: 8px;
        color: #1976d2;
        font-size: 11pt;
    }
""")

# 优化为更现代的风格
hint_label.setStyleSheet("""
    QLabel {
        background-color: #E8F5E9;
        border-left: 4px solid #4CAF50;
        border-radius: 4px;
        padding: 12px 16px;
        color: #2E7D32;
        font-size: 10pt;
    }
""")
```

### 4. 分组框 (QGroupBox) 优化

```python
# 当前样式 - 默认灰色边框
behavior_group = QGroupBox("⚡ 今日行为摘要")

# 优化为现代卡片样式
behavior_group.setStyleSheet("""
    QGroupBox {
        background-color: #FFFFFF;
        border: 1px solid #E0E0E0;
        border-radius: 8px;
        margin-top: 12px;
        padding: 16px;
        font-weight: bold;
        font-size: 11pt;
    }
    QGroupBox::title {
        subcontrol-origin: margin;
        subcontrol-position: top left;
        left: 12px;
        top: 6px;
        color: #2196F3;
    }
""")
```

### 5. 统计卡片优化

```python
# 为任务统计卡片添加阴影和悬停效果
total_card.setStyleSheet("""
    QWidget {
        background-color: #E3F2FD;
        border: 1px solid #90CAF9;
        border-radius: 8px;
        padding: 12px;
    }
    QWidget:hover {
        background-color: #BBDEFB;
        border-color: #64B5F6;
    }
""")
```

### 6. 按钮样式优化

```python
# 刷新按钮
refresh_button.setStyleSheet("""
    QPushButton {
        background-color: #2196F3;
        color: white;
        border: none;
        border-radius: 4px;
        padding: 8px 16px;
        font-size: 10pt;
        font-weight: bold;
    }
    QPushButton:hover {
        background-color: #1976D2;
    }
    QPushButton:pressed {
        background-color: #0D47A1;
    }
""")

# 导出按钮
export_button.setStyleSheet("""
    QPushButton {
        background-color: #4CAF50;
        color: white;
        border: none;
        border-radius: 4px;
        padding: 8px 16px;
        font-size: 10pt;
        font-weight: bold;
    }
    QPushButton:hover {
        background-color: #45A049;
    }
    QPushButton:pressed {
        background-color: #3D8B40;
    }
""")
```

### 7. 进度条优化

```python
# 行为占比进度条
self.behavior_ratio_bar.setStyleSheet("""
    QProgressBar {
        border: 1px solid #E0E0E0;
        border-radius: 6px;
        text-align: center;
        background-color: #F5F5F5;
        height: 24px;
        font-size: 10pt;
        font-weight: bold;
        color: #2C3E50;
    }
    QProgressBar::chunk {
        background-color: qlineargradient(
            x1:0, y1:0, x2:1, y2:0,
            stop:0 #4CAF50, stop:1 #66BB6A
        );
        border-radius: 5px;
    }
""")
```

### 8. 表格样式优化

```python
table.setStyleSheet("""
    QTableWidget {
        border: 1px solid #E0E0E0;
        border-radius: 4px;
        gridline-color: #F0F0F0;
        background-color: #FFFFFF;
        color: #2C3E50;
        selection-background-color: #E3F2FD;
        selection-color: #1976D2;
    }
    QTableWidget::item {
        padding: 8px;
        border-bottom: 1px solid #F0F0F0;
    }
    QTableWidget::item:hover {
        background-color: #F5F5F5;
    }
    QHeaderView::section {
        background-color: #FAFAFA;
        color: #2C3E50;
        padding: 10px;
        border: none;
        border-bottom: 2px solid #E0E0E0;
        font-weight: bold;
        font-size: 10pt;
    }
""")
```

## 📝 实施步骤

1. ✅ **第一步**: 修改 `apply_theme()` 方法
   - 统一背景色为浅色系
   - 优化标签页样式
   - 优化按钮样式

2. ✅ **第二步**: 优化卡片和分组框样式
   - 添加现代化的边框和圆角
   - 统一padding和margin
   - 添加悬停效果

3. ✅ **第三步**: 优化进度条和表格
   - 使用渐变色增强视觉效果
   - 统一边框和圆角样式
   - 优化选中状态的颜色

4. ✅ **第四步**: 测试和微调
   - 在不同主题下测试
   - 调整细节确保一致性
   - 确保深色模式也能正常工作

## 🎯 预期效果

优化后的统计报告界面将具备:

- ✅ **统一的视觉风格** - 与配置界面无缝衔接
- ✅ **更好的可读性** - 浅色背景,清晰的文字对比度
- ✅ **现代化的UI** - Material Design风格的卡片和按钮
- ✅ **流畅的交互** - 悬停效果,按下反馈
- ✅ **主题适配** - 支持多主题切换

## 📊 配色方案总结

| 元素 | 颜色 | 用途 |
|-----|------|------|
| **主背景** | `#F5F5F5` | 窗口整体背景 |
| **卡片背景** | `#FFFFFF` | 内容卡片背景 |
| **边框** | `#E0E0E0` | 统一边框颜色 |
| **主色** | `#2196F3` | 标题、链接、选中状态 |
| **成功** | `#4CAF50` | 完成状态、正向操作 |
| **警告** | `#FF9800` | 待确认、需要注意 |
| **错误** | `#F44336` | 错误状态、危险操作 |
| **文字** | `#2C3E50` | 主要文字颜色 |
| **次要文字** | `#6C757D` | 辅助说明文字 |

---

**生成时间**: 2025-12-08
**版本**: v1.0
